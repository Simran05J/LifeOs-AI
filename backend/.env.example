# App Configuration
PROJECT_NAME="AI Life OS Backend"
APP_ENV="development"
DEBUG=True
API_V1_STR="/api/v1"

# Security
SECRET_KEY="your-secret-key-for-jwt-signing"
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Firebase Configuration (JSON credentials can be placed in firebase_credentials.json or set as environment variables)
FIREBASE_CREDENTIALS_PATH="app/firebase/firebase_credentials.json"
FIREBASE_CREDENTIALS_JSON='{"type": "service_account", "project_id": "...", ...}'
FIREBASE_DATABASE_URL="https://your-project-id.firebaseio.com"

